README.md
*pkglist.txt

# If NOT my machine, ignore everything except the allowed directories
{{- if ne .chezmoi.hostname "fort-gael" }}

*
.config/*

!.tmux.conf
!.config
!.config/nvim
!.config/fish

{{- end }}
